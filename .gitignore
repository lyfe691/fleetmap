# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# osrm dataset (large; built locally — see docker-compose.yml)
/osrm

# brainstorming visual companion
/.superpowers

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# ai
.claude/

# design handoff + scratch assets (reference only — not part of the build)
/design_handoff_fleetmap/
/tmp-image/
# local-only handoff doc (contains client keys; shared privately, never committed)
docs/driver-app-handoff.md
